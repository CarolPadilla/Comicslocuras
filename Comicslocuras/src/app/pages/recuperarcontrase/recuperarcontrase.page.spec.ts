import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RecuperarcontrasePage } from './recuperarcontrase.page';

describe('RecuperarcontrasePage', () => {
  let component: RecuperarcontrasePage;
  let fixture: ComponentFixture<RecuperarcontrasePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RecuperarcontrasePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
